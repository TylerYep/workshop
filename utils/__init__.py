"""
To run files in this directory, use `python -m utils.<file name here>`
e.g. `python -m utils.hash_table`

Alternatively, you can add the following line to the top of the file:
```
import sys
sys.path.append("../")
```
Then, run the file inside of explore/, as in `python hash_table.py`.
"""
